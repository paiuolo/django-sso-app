============
Installation
============

At the command line::

    $ easy_install django-sso-app

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sso-app
    $ pip install django-sso-app
